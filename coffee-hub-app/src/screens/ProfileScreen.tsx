import { ScreenPlaceholder } from '../components/ScreenPlaceholder';

export function ProfileScreen() {
  return (
    <ScreenPlaceholder
      title="Profile"
      subtitle="Profile navigation is in place so we can plug in auth and account screens next."
    />
  );
}
